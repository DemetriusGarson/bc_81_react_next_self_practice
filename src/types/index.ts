export interface User {
    name: string;
    email: string;
    isOnline: boolean;
    id: string;
}

export interface Book {
    author: string;
    title: string;
    year: string;
    description: string;
    id: string;
}